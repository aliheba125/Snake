# الخلاصة النهائية الموحّدة — تحليل Snake Engine (SE_2.2.6)

> يجمع هذا الملف نتائج كل المراحل: تحليل ثابت (jadx/apktool/Blutter) + تحليل أصلي (Ghidra) + تحليل ديناميكي (Qiling/Unicorn)، مع **تدرّج ثقة صريح** لكل استنتاج.

---

## 0. سلوك التطبيق وقت التشغيل (ماذا يحمّل / يعدّل)

| النشاط وقت التشغيل | الدليل | الثقة |
|--------------------|--------|-------|
| **يحمّل مكتبة أصلية** `libengine.so` عند الإقلاع | `App.java: System.loadLibrary("engine")` | 🟢 مُثبَت |
| **يحمّل كود اللعبة المستهدفة** داخل حاويته | محمّلات `PathClassLoader` مخصّصة + `createPackageContext`×11 | 🟢 مُثبَت |
| **يخصّص ذاكرة قابلة للتنفيذ (RWX) ويولّد كودًا فيها** | Ghidra: `mmap(prot=7)`+كتابة `\|0x14000000` — و Qiling رصد 39× mmap RWX فعليًا | 🟢 مُثبَت (مصدران) |
| **يعدّل/يهوك دوال Java** | `fh` يزيّف `Executable/MethodHandle/Class`؛ `Native.update(Object,Method)` | 🟢 مُثبَت |
| **يعيد تعيين هوية العملية** (UID/PID) | `Native.gcuid()` → `Binder.getCallingPid()` | 🟢 مُثبَت |
| **فحوص توقيت (مقاومة تحليل)** | Qiling رصد `clock_gettime`×36 أثناء التهيئة | 🟢 مُثبَت |
| **يفكّ سلاسل/بيانات وقت التشغيل** | `Native.ilil(int)→String`، `djp(int)→byte[]` | 🟡 دليل قوي |
| **يفحص ترخيصًا/توقيعًا** | `Native.chl(byte[])→bool` + فحوص الاشتراك | 🟡 دليل قوي |
| **يتصل بخادم خلفي** | `rest.snakeseller.com/api/request/` (طبقة Dart) | 🟢 مُثبَت |
| **خدمة دائمة (Persistence)** | `DaemonService` يُعيد جدولة نفسه | 🟢 مُثبَت |

**الخلاصة:** نعم — التطبيق **يحمّل مكتبة أصلية + كود لعبة مستهدفة**، **يخصّص ذاكرة تنفيذية ويولّد كودًا (Self-modifying)**، **يهوك دوال Java ويزيّف فئات الانعكاس**، **يعيد تعيين هوية العملية**، ويطبّق **مقاومة تحليل** (فحوص توقيت + تشفير سلاسل + نداءات نظام مباشرة).

---

## 1. ما هو التطبيق (تقسيم معماري مُثبَت)

```
Snake Engine (com.snake) v2.2.6  — أداة تعديل ألعاب تُباع باشتراكات
│
├── libapp.so  (Flutter/Dart)  = الواجهة الأمامية / لوحة الإدارة
│     • لوحة البائع: Global Seller, Code Sale, Account Manager, إنشاء/تفاصيل المفاتيح
│     • الاشتراكات والتفعيل المرتبط بالجهاز: Enter code, Add Device, Device removed, نظام الحظر
│     • اختيار اللعبة (Game Selection)  • فاحص التحديثات  • تسجيل دخول Facebook
│     • يتصل بـ rest.snakeseller.com/api/request/
│
└── libengine.so  (Native C/C++, مُعتَّم بعنف)  = المحرّك الفعلي
      • حاوية محاكاة تطبيقات (App-Virtualization) تشغّل اللعبة المختارة داخلها
      • محرّك Hooking أصلي (Inline Hook) يطبّق التعديلات على اللعبة وقت التشغيل
      • يولّد كودًا في ذاكرة RWX، يهوك دوال Java، يعيد تعيين UID/PID
```

---

## 2. مصفوفة الأدلة (كل الأدوات)

| المرحلة | الأداة | الناتج | القيمة |
|---------|--------|--------|--------|
| تحليل Java | jadx 1.5.1 | `java-jadx/` (3086 ملف .java) | 🟢 كود مقروء (مبهم) |
| موارد/مانيفست | apktool 2.10.0 | `smali/`, `resources/`, Manifest | 🟢 كامل |
| كود Dart | Blutter (+Dart VM 3.5.4) | `dart-blutter/` (670 ملف) | 🟡 مبهم الأسماء |
| فكّ الكود الأصلي | **Ghidra 12.1.2** | `ghidra/libengine_decompiled.c` (2241 من 2283 دالة معرّفة؛ 2418 إجمالي شامل الخارجية) | 🟢 C فعلي |
| تحليل ELF/بنية | pyelftools/capstone | `NATIVE_DEEP_ANALYSIS.md` | 🟢 |
| **محاكاة ديناميكية** | **Qiling 1.4.6/Unicorn** | `emulation/emulation_trace*.txt` | 🟢 سلوك حقيقي جزئي |

---

## 3. حدود التحليل

### 3.1 لماذا لا محاكي أندرويد كامل؟
البيئة **بلا KVM ولا تسريع عتادي**، والمكتبة **ARM64 فقط** (لا نسخة x86). لذلك محاكي أندرويد كامل (بواجهة + Frida) **غير ممكن تقنيًا هنا** — وليس خيارًا أهملته.

### 3.2 لماذا توقّفت المحاكاة العميقة؟ (جدار مقاومة المحاكاة)
بنينا طبقة نداءات نظام حقيقية (mmap يخصّص فعليًا، mprotect ينجح، تسجيل المسارات...). النتيجة:
- بملء ذاكرة RWX بأصفار → المُنشئات **تتعطّل بعد ~94 تعليمة** (تقفز إلى كعب لم يُكتَب = تعليمة غير معرّفة).
- بملئها بـ`RET` → المُنشئات **تدخل حلقات لا نهائية** (dispatchers مُسطّحة تحتاج قيمًا مفكوكة من بيئة حقيقية لتخرج).
- **الاستنتاج:** هذا سلوك مقاومة مُصمَّم عمدًا؛ فكّ السلاسل مربوط بحالة لا تكتمل خارج جهاز أندرويد حقيقي.

### 3.3 ما لم نستطع استخراجه ساكنًا/بالمحاكاة (ويحتاج جهازًا حقيقيًا)
- أسماء دوال JNI الفعلية (تُسجَّل عبر `RegisterNatives` وقت التشغيل بأسماء مفكوكة).
- السلاسل المفكوكة داخل النواة الأصلية.
- الملفات الفعلية المُنشأة وحركة الشبكة الكاملة (تحدث بعد التهيئة المحميّة).

### 3.4 تصحيحات دقّة
- «25,804 نداء نظام» من الفكّ الساكن = **أثر خادع غير موثوق** — مرفوض.
- «OLLVM» = **تعتيم شبيه بـ OLLVM** (دليل قوي، لا جزم بأداة).
- «توليد الكود وقت التشغيل» = **مُثبَت الآن** (Ghidra + Qiling)، لم يعد استنتاجًا.

---

## 4. البيانات غير القابلة للاستخراج ساكنًا
العناصر التالية تُبنى/تُفكّ وقت التشغيل فقط ولا تظهر في التحليل الساكن أو المحاكاة الجزئية:
- خريطة `RegisterNatives` (اسم الدالة الأصلي ↔ المؤشّر).
- السلاسل المفكوكة داخل النواة الأصلية.
- الملفات المُنشأة فعليًا وحمولة الشبكة بعد التشفير (`encryptedData`).
